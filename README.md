# @paragraphcms/parser-svelte

Svelte helpers for rendering Paragraph CMS content.

This package renders Paragraph CMS `markdown`, `html`, and `tiptap` content to sanitized HTML strings for Svelte usage with `{@html}`.
